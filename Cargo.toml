[package]
name = "endless_sky_rw"
version = "0.2.1"
edition = "2024"

[lints.clippy]
nursery     = { level  = "deny", priority = -1 }
pedantic    = { level  = "deny", priority = -1 }
all         = { level  = "deny", priority = -1 }
unwrap_used = "deny"

[profile.release]
lto = true

[dependencies]
